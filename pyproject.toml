[project]
name = "workspaceclerk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.3.2",
    "tomli-w>=1.2.0",
]

[tool.uv.workspace]
members = [
    "app1",
]
