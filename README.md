# Workspace Clerk ![Version](https://img.shields.io/badge/version-0.1.0-blue)

**Package management assistant without a headache.**

Консольный помощник для управления локальными пакетами и зависимостями в Python-проектах на основе `uv`. Вместо
запоминания цепочек команд — простое интерактивное меню.

## Что делает

Инструмент сканирует проект и позволяет управлять им через консоль:

- **Находит** локальные Python-пакеты в указанной директории.
- **Управляет подключением**: подключает и отключает пакеты в основном проекте.
- **Контролирует зависимости**: добавляет и удаляет библиотеки для всего проекта или для отдельных пакетов.
- **Показывает статус**: отображает список всех пакетов с их статусом (включен/выключен) и установленными зависимостями.

## Быстрый старт

### Предварительные требования

- Установленный и доступный в системе менеджер пакетов **[`uv`](https://github.com/astral-sh/uv)**.
- Python 3.11 или новее.

### Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/MikeNew2026/workspaceClerk.git
   cd WorkspaceClerk
   ```
2. *(Опционально)* Создайте и активируйте виртуальное окружение с помощью `uv`:
   ```bash
   uv venv
   source .venv/bin/activate  # На Windows: .venv\Scripts\activate
   ```
   Важно! uv создаёт python связанный с глобальным интерпретатором.

3. Установите зависимости проекта (включая `tomli-w`):
   ```bash
   uv sync
   ```
4. Запустите утилиту:
   ```bash
   python main.py
   ```

## 📄 Лицензия

Этот проект распространяется под лицензией [MIT License](LICENSE).

Полный текст лицензии доступен в файле [`LICENSE`](LICENSE) в корне репозитория. Кратко: вы можете свободно использовать, копировать, изменять и распространять этот код, в том числе в коммерческих целях, при условии сохранения исходного уведомления об авторских правах и текста самой лицензии.

---
**Проект находится в активной разработке. Ваши идеи и предложения очень приветствуются!**

